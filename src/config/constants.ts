/**
 * Application-wide constants
 */

// CPM (Cost Per Mille) Configuration
export const CPM_RATE = 1.5; // $1.50 per 1000 views

// CPM Tracking Window
export const CPM_WINDOW_DAYS = 28; // Posts earn CPM for 4 weeks after submission

// Apify Configuration
export const APIFY_RETRY_ATTEMPTS = 5;
export const APIFY_TIMEOUT_MS = 300000; // 5 minutes

// Analytics
export const VIRALITY_THRESHOLD = 100000; // Views threshold for virality alerts
